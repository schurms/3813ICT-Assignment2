// import { Channel } from './channel.model';

export class Group {
  id: number;
  name: string;
  // channel: Channel;

  constructor( id: number, name: string ) {
    this.id = id;
    this.name = name;
  }

}
